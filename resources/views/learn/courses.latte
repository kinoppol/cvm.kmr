{layout '../layout.latte'}

{block title}วิชาที่เรียน{/block}

{block content}
<div class="dash">
    <div class="dash-greeting">วิชาที่เรียน</div>
    <div class="dash-sub">{$term ?? ''} · {count($courses)} รายวิชา</div>

    {if $courses}
        <div class="course-grid">
            {foreach $courses as $c}
                <a class="course-card" href="{$base}/learn/{$c['id']}">
                    <div class="course-card-head" style="background: var(--brand)">
                        <div class="code">{$c['code']}</div>
                        <div class="name">{$c['name']}</div>
                    </div>
                    <div class="course-card-body">
                        <div class="meta">ผู้สอน: {$c['teacher_name'] ?? '—'}</div>
                    </div>
                </a>
            {/foreach}
        </div>
    {else}
        <div class="empty" style="margin-top:26px"><h3>ยังไม่ได้ลงทะเบียนในรายวิชาใด</h3><p>ติดต่อครูประจำวิชาเพื่อเพิ่มชื่อเข้ารายวิชา</p></div>
    {/if}
</div>
{/block}
